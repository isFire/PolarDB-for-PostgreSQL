# =============================================================================
# PolarDB Docker 最小扩展配置示例
# =============================================================================
# 仅包含最基础的 PolarDB 监控扩展
# 适合测试和开发环境
# =============================================================================

extension_sets:
  minimal:
    - polar_monitor
    - polar_audit

default_sets: [minimal]
